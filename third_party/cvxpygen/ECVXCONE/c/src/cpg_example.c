
/*
Auto-generated by CVXPYgen on August 01, 2025 at 11:48:33.
Content: Example program for updating parameters, solving, and inspecting the result.
*/

#include <stdio.h>
#include "cpg_workspace.h"
#include "cpg_solve.h"

static int i;

int main(int argc, char *argv[]){

  // Update first entry of every user-defined parameter
  cpg_update_A(0, 1.00000000000000000000);
  cpg_update_B(0, 0.00000000000000000000);
  cpg_update_tracking_err_square(0, 0.00000000000000000000);

  // Solve the problem instance
  cpg_solve();

  // Print objective function value
  printf("obj = %f\n", CPG_Result.info->obj_val);

  // Print primal solution
  for(i=0; i<100; i++) {
    printf("Q[%d] = %f\n", i, CPG_Result.prim->Q[i]);
  }
  for(i=0; i<60; i++) {
    printf("R[%d] = %f\n", i, CPG_Result.prim->R[i]);
  }
  for(i=0; i<36; i++) {
    printf("T[%d] = %f\n", i, CPG_Result.prim->T[i]);
  }

  // Print dual solution
  for(i=0; i<400; i++) {
    printf("d0[%d] = %f\n", i, CPG_Result.dual->d0[i]);
  }
  for(i=0; i<256; i++) {
    printf("d1[%d] = %f\n", i, CPG_Result.dual->d1[i]);
  }
  for(i=0; i<100; i++) {
    printf("d2[%d] = %f\n", i, CPG_Result.dual->d2[i]);
  }
  for(i=0; i<9; i++) {
    printf("d3[%d] = %f\n", i, CPG_Result.dual->d3[i]);
  }
  for(i=0; i<36; i++) {
    printf("d4[%d] = %f\n", i, CPG_Result.dual->d4[i]);
  }
  for(i=0; i<36; i++) {
    printf("d5[%d] = %f\n", i, CPG_Result.dual->d5[i]);
  }

  return 0;

}
